import React from "react";
import styles from "./styles/styles.module.css";

function Font(): JSX.Element
{
    return (
        <div className={styles["text-font"]}>Arial</div>
    );
}

export default Font;