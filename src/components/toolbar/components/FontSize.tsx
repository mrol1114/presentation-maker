import React from "react";
import styles from "./styles/styles.module.css";

function FontSize(): JSX.Element
{
    return (
        <div className={styles["text-font-size"]}>
            число
        </div>
    );
}

export default FontSize;